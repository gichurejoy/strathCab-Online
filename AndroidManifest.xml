<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.strathcab">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/strath_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/strath_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StrathCab">
        <activity
            android:name=".AddProfileImageCustomer"
            android:theme="@style/LoginAndSignup"
            android:exported="true" />
        <activity
            android:name=".updateProfile"
            android:exported="true" />
        <activity
            android:name=".OfferRide"
            android:exported="true" />
        <activity
            android:name=".ViewProfileDriver"
            android:exported="true" />
        <activity
            android:name=".FindRide2"
            android:exported="true" />
        <activity
            android:name=".FindRide"
            android:exported="true" />
        <activity
            android:name=".DriverMain"
            android:exported="true"
            android:theme="@style/LoginAndSignup" />
        <activity
            android:name=".ViewProfileCustomer"
            android:exported="true" />
        <activity
            android:name=".CustomerMain"
            android:exported="true"
            android:theme="@style/LoginAndSignup" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".CarProfileActivity"
            android:exported="true"
            android:theme="@style/LoginAndSignup" />
        <activity
            android:name=".DriverSignup"
            android:theme="@style/LoginAndSignup" />
        <activity
            android:name=".DriverLogin"
            android:theme="@style/LoginAndSignup" />
        <activity
            android:name=".DriverMaps"
            android:label="@string/title_activity_driver_maps" />
        <activity android:name=".ForgotPassword" />
        <activity
            android:name=".CustomerSignup"
            android:theme="@style/LoginAndSignup" />
        <activity
            android:name=".CustomerLogin"
            android:theme="@style/LoginAndSignup" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>